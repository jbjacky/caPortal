import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SearchChangeFormComponent } from './search-change-form.component';

describe('SearchChangeFormComponent', () => {
  let component: SearchChangeFormComponent;
  let fixture: ComponentFixture<SearchChangeFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SearchChangeFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchChangeFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
