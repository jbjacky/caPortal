import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReviewformPhoneComponent } from './reviewform-phone.component';

describe('ReviewformPhoneComponent', () => {
  let component: ReviewformPhoneComponent;
  let fixture: ComponentFixture<ReviewformPhoneComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReviewformPhoneComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReviewformPhoneComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
