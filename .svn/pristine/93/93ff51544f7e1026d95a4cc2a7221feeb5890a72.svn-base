import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SignProxymanComponent } from './sign-proxyman.component';

describe('SignProxymanComponent', () => {
  let component: SignProxymanComponent;
  let fixture: ComponentFixture<SignProxymanComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SignProxymanComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SignProxymanComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
