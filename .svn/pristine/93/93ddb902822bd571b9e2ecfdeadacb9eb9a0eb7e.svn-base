import { SharemodelModule } from './sharemodel.module';

describe('SharemodelModule', () => {
  let sharemodelModule: SharemodelModule;

  beforeEach(() => {
    sharemodelModule = new SharemodelModule();
  });

  it('should create an instance', () => {
    expect(sharemodelModule).toBeTruthy();
  });
});
