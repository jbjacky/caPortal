import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SelectSignerComponent } from './select-signer.component';

describe('SelectSignerComponent', () => {
  let component: SelectSignerComponent;
  let fixture: ComponentFixture<SelectSignerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SelectSignerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SelectSignerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
