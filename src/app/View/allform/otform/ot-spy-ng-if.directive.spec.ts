import { OtSpyNgIfDirective } from './ot-spy-ng-if.directive';

describe('OtSpyNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OtSpyNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
