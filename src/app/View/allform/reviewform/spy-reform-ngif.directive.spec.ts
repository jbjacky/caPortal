import { SpyReformNgifDirective } from './spy-reform-ngif.directive';

describe('SpyReformNgifDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyReformNgifDirective();
    expect(directive).toBeTruthy();
  });
});
