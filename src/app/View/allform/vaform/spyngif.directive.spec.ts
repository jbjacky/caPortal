import { SpyngifDirective } from './spyngif.directive';

describe('SpyngifDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyngifDirective();
    expect(directive).toBeTruthy();
  });
});
