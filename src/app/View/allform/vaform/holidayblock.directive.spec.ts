import { HolidayblockDirective } from './holidayblock.directive';

describe('HolidayblockDirective', () => {
  it('should create an instance', () => {
    const directive = new HolidayblockDirective();
    expect(directive).toBeTruthy();
  });
});
