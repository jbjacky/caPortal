import { ShowAttendDirective } from './show-attend.directive';

describe('ShowAttendDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAttendDirective();
    expect(directive).toBeTruthy();
  });
});
