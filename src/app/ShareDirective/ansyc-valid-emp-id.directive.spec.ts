import { AsyncValidEmpIDDirective } from "./ansyc-valid-emp-id.directive";

describe('ValidEmpIDDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidEmpIDDirective();
    expect(directive).toBeTruthy();
  });
});
